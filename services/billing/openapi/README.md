# Billing API

The executable Stage 3 order, payment, and YooKassa webhook operations are documented in `contracts/http/openapi.yaml`. Billing event payloads are documented in `contracts/events/asyncapi.yaml` and validated against versioned JSON Schemas under `contracts/events/schemas`.
