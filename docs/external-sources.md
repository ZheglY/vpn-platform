# External Sources Checked

Checked through 2026-07-25.

These sources are used only to shape Stage 0 decisions. Implementation stages must re-check the relevant official documentation before coding an integration.

| Area | Official source | Stage 0 finding |
|---|---|---|
| Codex instructions | https://developers.openai.com/codex/guides/agents-md | Codex reads repository `AGENTS.md` as project guidance, so this repository keeps rules compact and points to detailed docs. |
| Telegram Bot API | https://core.telegram.org/bots/api | Stage 7 re-checked the current official Bot API on 2026-07-19: `sendMessage` requires `chat_id` and 1-4096 characters after entity parsing; Stage 7 fixes HTML parse mode and escapes all dynamic values. |
| Telegram Bot API response envelope | https://core.telegram.org/bots/api | Stage 7 re-checked `ok`, `result`, `error_code`, `description`, and optional `ResponseParameters.retry_after` before implementing retry/permanent error classification. Telegram does not provide an application idempotency key for `sendMessage`, so ADR 0025 does not claim exactly-once delivery. |
| Go `net/url` error type | https://pkg.go.dev/net/url#Error | Stage 2 re-checked that `url.Error` carries the request URL, so Telegram client errors must not wrap it when the URL contains the bot token. |
| YooKassa payment process | https://yookassa.ru/developers/payment-acceptance/getting-started/payment-process | Payment creation uses authentication, `Idempotence-Key`, amount, confirmation data, and status transitions such as `pending`, `succeeded`, and `canceled`. |
| YooKassa webhooks | https://yookassa.ru/developers/using-api/webhooks | Re-checked 2026-07-18: acknowledge with HTTP 200, verify the current provider object, and optionally verify the documented IPv4/IPv6 source ranges. Production requires the current official source list at deployment time, not a stale application constant. |
| YooKassa response handling | https://yookassa.ru/developers/using-api/response-handling/http-codes | Ambiguous provider errors require retry with the same idempotency key or GET verification rather than assuming success or failure. |
| YooKassa API interaction and idempotency | https://yookassa.ru/developers/using-api/interaction-format | Re-checked 2026-07-18: `Idempotence-Key` is at most 64 characters, UUID v4 is recommended, and a result is retained for 24 hours. The implementation persists a UUID before I/O and centrally enforces a 23-hour create deadline. |
| YooKassa quick start | https://yookassa.ru/developers/payment-acceptance/getting-started/quick-start | Stage 3 uses Basic Auth, integer-to-decimal amount rendering, immediate capture, redirect confirmation, and minimal internal metadata. |
| YooKassa testing | https://yookassa.ru/developers/payment-acceptance/testing-and-going-live/testing | Stage 3 requires a test shop/payment object and rejects any provider response without `test=true`; no real money or production credentials are used. |
| YooKassa webhook delivery | https://yookassa.ru/developers/using-api/webhooks | Stage 3 acknowledges only after durable normalized inbox storage and verifies current object state through an authenticated API request before a terminal effect. Non-2xx delivery may be retried for up to 24 hours. |
| Happ developer docs | https://www.happ.su/main/dev-docs | Happ supports subscription delivery through a URL returning proxy links and parameters. |
| Happ app management | https://www.happ.su/main/dev-docs/app-management | Stage 5 re-checked 2026-07-18: Happ accepts `profile-title` (plain text or base64, max 25 characters), hourly `profile-update-interval`, semicolon-separated `subscription-userinfo`, and `support-url` as headers or body directives. The official page includes VLESS + REALITY URI examples. Provider ID is needed only for advanced controls and is intentionally omitted. |
| Happ link examples | https://www.happ.su/main/dev-docs/examples-of-links-and-parameters | Stage 5 golden tests use a plain-text list with one VLESS URI per endpoint and standard headers; no HWID or Provider ID behavior is enabled. |
| Xray-core | https://github.com/XTLS/Xray-core | The official Xray-core project is the source for pinned release selection, config validation, and security review. |
| gRPC-Go advisory | https://github.com/advisories/GHSA-hrxh-6v49-42gf | Re-checked 2026-07-23: versions below 1.82.1 are affected by HIGH xDS RBAC authorization/panic and HTTP/2 denial-of-service issues. The integrity-pinned Xray source is security-rebuilt with gRPC-Go 1.82.1 because the latest reviewed official Xray release still used an affected version. |
| Xray examples | https://github.com/XTLS/Xray-examples | VLESS + REALITY examples are reference material for Stage 5/6 golden tests, not a substitute for implementation validation. |
| Xray VLESS outbound | https://xtls.github.io/en/config/outbounds/vless.html | Stage 5 re-checked 2026-07-18: client IDs are UUIDs, VLESS encryption is explicitly `none`, and public deployment requires an authenticated transport such as REALITY. |
| Xray REALITY transport | https://xtls.github.io/en/config/transports/reality.html | Stage 5 re-checked 2026-07-18: current client fields include server name, fingerprint, public-side password/key, short ID, and optional spider path; short IDs are even-length hexadecimal up to 16 characters. REALITY private keys remain server-side. |
| XTLS share-link proposal | https://github.com/XTLS/Xray-core/discussions/716 | Stage 5 uses the official XTLS VLESS URI parameter mapping (`security=reality`, `pbk`, `sid`, optional `spx`) and URL encoding in deterministic golden tests. |
| Go releases/downloads | https://go.dev/dl/ | Stage 1 pins stable Go 1.26.5 after `govulncheck` found standard-library vulnerabilities in 1.26.2. Release candidates are not used. |
| Go vulnerability database | https://go.dev/security/vuln/database | Stage 1 `make vuln` uses the official Go vulnerability database through `govulncheck`; the Windows fallback mirrors only the needed official JSON endpoints into a temporary local database. |
| govulncheck versions | `go list -m -versions golang.org/x/vuln` | Stage 1 pins `golang.org/x/vuln/cmd/govulncheck` v1.6.0. |
| Gitleaks versions | `go list -m -versions github.com/gitleaks/gitleaks/v8` and module redirect to `github.com/zricethezav/gitleaks/v8` | Stage 1 pins Gitleaks v8.30.1 for local and CI secret scanning. |
| Trivy image | `docker run aquasec/trivy:latest --version` | Stage 1 verified the current Trivy image reports version 0.72.0, and `make image-scan` pins `aquasec/trivy:0.72.0`. |
| GitHub Actions refs | `git ls-remote` for `actions/checkout`, `actions/setup-go`, and `actions/setup-node` | Stage 1 pins the v5/v6/v6 action refs by full commit SHA in CI. |
| Local container image digests | `docker buildx imagetools inspect` | Stage 1 pins `postgres:18-alpine`, `redis:8-alpine`, and `apache/kafka:4.3.1` by manifest digest in Compose. |
| Prometheus configuration and alerting rules | https://prometheus.io/docs/prometheus/latest/configuration/configuration/ and https://prometheus.io/docs/prometheus/latest/configuration/alerting_rules/ | Stage 8 re-checked configuration validation, the accepted `TLS13` scrape value, rule syntax, `for`, labels, and annotations before adding protected profile-specific inventories and runbook-linked absence/count alerts. |
| Prometheus releases and security rebuild | https://github.com/prometheus/prometheus/releases and official tag `v3.13.1` | Re-checked 2026-07-24 with the official Git refs: `3c2a2ff7aec85b531d74954cc99da75844dc106a` is the annotated tag object and `73ff57ce2b8161059ac7fe5188f03f1c3d22b29a` is the peeled release commit. The build pins the peeled commit archive SHA-256 `322276709c16cb3c22a4d01d244beb91f0380a257699358d2a20ac7086d7256c` and web UI SHA-256 `2194bfbb5d36457df2b8f480037ce89786ee32f03b5ee6ad5597989f14deafb0`, rebuilds with Go 1.26.5 and gRPC-Go 1.82.1, and retains upstream `LICENSE` and `NOTICE`. Trivy HIGH/CRITICAL scan and `promtool` are required gates. |
| Grafana provisioning and Docker installation | https://grafana.com/docs/grafana/latest/administration/provisioning/ and https://grafana.com/docs/grafana/latest/setup-grafana/installation/docker/ | Stage 8 provisions immutable data sources/dashboards from files and uses a loopback-only anonymous Viewer profile without a committed administrator password. |
| Grafana releases and security rebuild | https://github.com/grafana/grafana/releases and official tag `v13.1.1` | Re-checked 2026-07-25. Stage 8 pins commit `a9cee6e1724a455676bb6c05eef7fc54aa4b19f4`, source SHA-256 `722f7374891d375f0900dbee6e638d8b692edeba5d2353dd1d9315a3372cf78c`, and official runtime digest `sha256:7cb8c64c4d57a57e734073f3cc94620adb24a0acb929bd80ba9f14017e3a975b`. The security rebuild updates gRPC-Go and `kin-openapi`, copies only the verified Tempo protobuf tree needed by the server, removes unused bundled Zipkin/Elasticsearch backend executables, and must pass health/dashboard smoke plus a zero HIGH/CRITICAL Trivy gate. |
| `kin-openapi` advisory GHSA-r277-6w6q-xmqw | https://github.com/advisories/GHSA-r277-6w6q-xmqw and https://github.com/getkin/kin-openapi/releases/tag/v0.144.0 | Re-checked 2026-07-25. Versions through 0.143.0 can fail open when `ValidationHandler.Load()` receives no authentication function. The Grafana security rebuild pins patched 0.144.0 and verifies the resulting binary with Trivy. |
| W3C Trace Context and OpenTelemetry propagation | https://www.w3.org/TR/trace-context/ and https://opentelemetry.io/docs/specs/otel/context/api-propagators/ | Re-checked 2026-07-25. Stage 8 propagates only valid `traceparent`/`tracestate` over HTTP and Kafka. Baggage is deliberately disabled and trace headers are not persisted in business envelopes or outbox rows. |
| OpenTelemetry Go exporters | https://opentelemetry.io/docs/languages/go/exporters/ and https://opentelemetry.io/docs/languages/go/instrumentation/ | Re-checked 2026-07-25. Go services use OTLP/HTTP protobuf exporters for traces and beta logs plus the W3C TraceContext propagator. Explicit signal paths avoid exporter-specific base-URL behavior. |
| OpenTelemetry Collector | https://opentelemetry.io/docs/collector/ and https://opentelemetry.io/docs/collector/transforming-telemetry/ | Re-checked 2026-07-25. The custom v0.157.0 distribution contains only reviewed receive/process/export/health components. Official Git refs identify annotated tag object `ebe705f0d7c3b99129ba65e405eafbf0341a79b0` and peeled release commit `4908404e59e544297b989a6961ee918b6f84b606`; the OCI revision uses the peeled commit. |
| Grafana Tempo local deployment and compaction | https://grafana.com/docs/tempo/latest/set-up-for-tracing/setup-tempo/deploy/locally/linux/ and https://grafana.com/docs/tempo/latest/operations/compaction/ | Re-checked 2026-07-25. Local single-binary Tempo 2.10.5 uses private OTLP ingress, filesystem blocks, and 720-hour trace retention. Tag object `a1dd9bfb94ee481e11da1694064c3f0feae3ab6b` peels to commit `991ce39eb956e9ed771fcffe05eff42d33de27ba`; the integrity-pinned source archive SHA-256 is `d8d1c1c7949343263621fa5d6b98030486841d1fb64622bbbbcb7ac21b593540`. The binary is security-rebuilt with Go 1.26.5, Prometheus 3.5 LTS module `v0.305.5`, and patched transitive dependencies into the pinned distroless runtime. |
| Prometheus Azure AD remote-write advisory and Trivy VEX | https://github.com/advisories/GHSA-wg65-39gg-5wfj, https://github.com/prometheus/prometheus/releases/tag/v3.5.5, and https://trivy.dev/docs/latest/configuration/filtering/ | Re-checked 2026-07-25. `CVE-2026-42151` was fixed in Prometheus 3.5.3 LTS because the OAuth client secret had been a plain string exposed through effective configuration. Tempo accepts the same remote-write type and serves `/status/config`, so the build uses current 3.5 LTS module `v0.305.5` and asserts the redacting `prometheus/common/config.Secret` field. Trivy's linear module range omits the LTS fix; one exact-PURL `vulnerable_code_not_present` statement corrects it, remains visible with `--show-suppressed`, and does not suppress other findings. |
| Grafana Loki OTLP ingestion and retention | https://grafana.com/docs/loki/latest/send-data/otel/ and https://grafana.com/docs/loki/latest/operations/storage/retention/ | Re-checked 2026-07-25. Collector exports to Loki's `/otlp` endpoint, structured metadata is enabled, index labels are explicitly bounded, and the singleton Compactor enforces 336-hour local retention. Loki 3.7.2 is pinned to lightweight-tag commit `7486c4a755a089363f07403dfa1c5fe727f25f4a`; the integrity-pinned source archive SHA-256 is `196c5d8214f5bdc8795fedf09a60f7418f4d098cb87edb4632c613abe21ed53d`. The binary is security-rebuilt with Go 1.26.5 and patched transitive dependencies into the pinned distroless runtime. |
| `brace-expansion` advisory GHSA-mh99-v99m-4gvg | https://github.com/advisories/GHSA-mh99-v99m-4gvg | Re-checked 2026-07-25. `@asyncapi/parser@3.6.0` still resolves affected `brace-expansion@1.1.16` through `spectral-core` and `minimatch@3`. Root tooling overrides `minimatch` to 10.2.5, which resolves patched `brace-expansion@5.0.8`; `npm audit` and all contract validators remain mandatory gates. |
| pgxpool statistics and pgx query tracing | https://pkg.go.dev/github.com/jackc/pgx/v5@v5.10.0/pgxpool and https://pkg.go.dev/github.com/jackc/pgx/v5@v5.10.0 | Re-checked 2026-07-24. Stage 8 uses the pinned pool `Stat` snapshot and query tracer interfaces, exporting only bounded operation/outcome labels and never SQL or arguments. |
| franz-go client hooks | https://pkg.go.dev/github.com/twmb/franz-go/pkg/kgo@v1.21.0 | Re-checked 2026-07-24. Stage 8 uses the pinned producer/fetch hook interfaces plus explicit owner worker observations; the hook contract does not authorize exporting record keys, payloads, headers, partitions, or offsets. |
| Prometheus instrumentation and naming practices | https://prometheus.io/docs/practices/instrumentation/ and https://prometheus.io/docs/practices/naming/ | Re-checked 2026-07-24. The second metrics slice follows bounded-label guidance, predeclares known state series, uses seconds as the duration unit, and exports custom collector health. |
| Prometheus SLO alerting and Alertmanager configuration | https://prometheus.io/docs/alerting/latest/configuration/ and https://prometheus.io/docs/alerting/latest/notifications/ | Re-checked 2026-07-25. Stage 8 validates grouped routes, inhibition, Go templates, and credential-free inert receivers with Alertmanager/amtool `v0.33.1`; production receiver configuration and escalation ownership remain unapproved. |
| age encryption | https://github.com/FiloSottile/age/releases/tag/v1.3.1 and https://pkg.go.dev/filippo.io/age@v1.3.1 | Re-checked 2026-07-25. The backup tool pins age v1.3.1, uses X25519 recipients, streams encryption/decryption, preserves the upstream license, and never commits a private identity. |
| PostgreSQL dump and restore | https://www.postgresql.org/docs/current/app-pgdump.html and https://www.postgresql.org/docs/current/app-pgrestore.html | Re-checked 2026-07-25 against PostgreSQL 18 client behavior. Backups use custom format with no owner/ACL, and clean restores use one transaction plus an explicitly provisioned service owner; global roles/passwords are infrastructure inputs rather than backup content. |
| Apache Kafka releases | https://kafka.apache.org/downloads | Stage 1 local Compose uses Apache Kafka in KRaft mode; image version must be revisited before production. |
| Go module registry | `go list -m -versions` | Stage 1 pinned current module versions for zap, pgx/v5, franz-go, go-redis/v9, Prometheus client, and goose. |
